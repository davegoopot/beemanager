[project]
name = "burst"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=4.2",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
